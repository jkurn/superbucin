services:
  - type: web
    name: superbucin
    runtime: node
    buildCommand: npm install && cd server && npm install && cd ../client && npm install && npx vite build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
